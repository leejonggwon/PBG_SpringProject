//페이지역할: BoardMapper 인터페이스는 게시판 데이터를 데이터베이스에서 조회하기 위한 매퍼 역할을 하며, 
//         MyBatis가 이 인터페이스를 통해 SQL 쿼리를 실행하고 결과를 반환하도록 합니다. 메서드 구현은 MyBatis에 의해 자동으로 처리됩니다.
//BoardMapper에 메서드를 만드는데 이름만 만들었지 구현은 MyBatis에서 한다 = 추상메서드
//추상클래스는 인터페이스로 만든다

package kr.spring.mapper;

import java.util.List;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Select;

import kr.spring.entity.Board;

@Mapper // MyBatis가 interface를 찾기위해 달아주는 부분
public interface BoardMapper {

	//get connection, sql문 실행, close등 마이바티스안에 있는 sqlsessionfactory에서 구현한다, 우린 sql문장만 작성하면 된다 
	@Select("SELECT * FROM BOARD")
	public List<Board> getLists(); //게시글 전체보기 기능 수행

	@Insert("INSERT INTO BOARD(TITLE, CONTENT, WRITER) VALUES()")
	public void boardInsert(Board board);
}



