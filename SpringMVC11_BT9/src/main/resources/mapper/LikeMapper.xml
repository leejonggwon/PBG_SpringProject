<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.spring.mapper.LikeMapper">
	
	<!--likeCount +1 기능-->
	<update id="likePlus" parameterType="Long">
		UPDATE BT_BOARD SET LIKE_COUNT = LIKE_COUNT + 1 WHERE IDX = #{idx}
	</update>
	
	<!--Like객체생성-->
	<insert id="insertLike" parameterType="kr.spring.entity.Like">
		INSERT INTO BT_LIKE(IDX, USERNAME)
		VALUES (#{idx}, #{username})               
	</insert>
	
	<!--like_available 값 불러오기-->
	<select id="selectLike" parameterType="kr.spring.entity.Like" resultType="int">
		SELECT LIKE_AVAILABLE FROM BT_LIKE WHERE IDX = #{idx} AND USERNAME = #{username}
	</select>
																					
	

</mapper>